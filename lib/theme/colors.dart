import 'package:flutter/material.dart';

class AppColors {
  static const Color red = Color(0xFFFF5757);      // Membaca
  static const Color blue = Color(0xFF4A90E2);     // Menulis
  static const Color yellow = Color(0xFFFFD93D);   // Berhitung
  static const Color green = Color(0xFF6BCF7F);    // Belajar Mode
  static const Color orange = Color(0xFFFF8B00);   // Tantangan
  static const Color softTeal = Color(0xFFE8F5F7); // Background Utama
  static const Color textDark = Color(0xFF030213);
  static const Color white = Colors.white;
}